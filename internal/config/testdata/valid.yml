settings:
  stable_for_seconds: 2
  on_conflict: rename
zones:
  - name: downloads
    path: ~/Downloads
    rules:
      - name: images
        match:
          extensions: [jpg, png]
        action:
          move: ~/Pictures
      - name: tmp
        match:
          glob: ["*.tmp"]
        action:
          delete: true
